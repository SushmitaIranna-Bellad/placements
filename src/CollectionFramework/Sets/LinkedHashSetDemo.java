package CollectionFramework.Sets;

import java.util.LinkedHashSet;

public class LinkedHashSetDemo {
    static void main(String[] args) {//everything is same as hash set but the one different thing is it maintain the order of insertion
        LinkedHashSet<Integer> set=new LinkedHashSet<>();


    }
}
/*it does not allow duplicates
*maintains the order of insertion -it acts like linkedlist(which Maintain the insertion order)
* uses hashing internally
* slightly slower than hashset
* no indexes
* rest every function is same has set
 */